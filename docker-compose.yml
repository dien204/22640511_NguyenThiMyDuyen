services:
  streamlit:
    build: .
    container_name: myduyen-streamlit
    ports:
      - "8501:8501"

  api:
    build: .
    container_name: myduyen-api
    ports:
      - "8000:8000"
    command: uvicorn src.api:app --host 0.0.0.0 --port 8000
